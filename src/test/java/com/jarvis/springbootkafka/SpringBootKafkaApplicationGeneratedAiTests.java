package com.jarvis.springbootkafka;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class SpringBootKafkaApplicationGeneratedAiTests {

    @Test
    void testMain() {
        // GIVEN - Context for the main method execution
        String[] args = new String[0];
        // WHEN - Call the main method under test
        SpringBootKafkaApplication.main(args);
        // THEN - Assert that the application started successfully (e.g., check logs or system state)
    }
}