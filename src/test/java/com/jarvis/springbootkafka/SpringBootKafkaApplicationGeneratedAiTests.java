package com.jarvis.springbootkafka;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.junit4.SpringRunner;

@SpringBootTest
class SpringBootKafkaApplicationGeneratedAiTests {

    @Test
    void testMain() {
        // GIVEN: The application context is loaded.
        // WHEN: The main method is executed.
        SpringRunner.run(SpringBootKafkaApplication.class, new String[] {}); 
        // THEN: The application starts successfully.
    }
}