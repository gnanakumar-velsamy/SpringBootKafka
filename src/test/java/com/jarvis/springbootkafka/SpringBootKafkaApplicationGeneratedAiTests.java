package com.jarvis.springbootkafka;

import org.junit.jupiter.api.Test;
import org.springframework.boot.SpringApplication;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class SpringBootKafkaApplicationGeneratedAiTests {

    @Test
    void testMain() {
        // GIVEN - Setup the context for testing the main method
        // WHEN - Execute the main method
        SpringApplication.run(SpringBootKafkaApplication.class, new String[0]);
        // THEN - Assert that the application started successfully (e.g., check for specific log messages or system properties) 
    }
}